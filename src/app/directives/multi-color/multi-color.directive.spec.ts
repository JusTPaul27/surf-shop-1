import { MultiColorDirective } from './multi-color.directive';

describe('MultiColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiColorDirective();
    expect(directive).toBeTruthy();
  });
});
